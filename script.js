# Web Starter Kit · Road to CE

ชุดเริ่มต้นทำเว็บง่าย ๆ สำหรับน้อง ๆ · มี 3 ไฟล์เชื่อมกันไว้ให้แล้ว เปิดปุ๊บใช้ได้ปั๊บ

```
index.html   ·  โครงสร้างหน้าเว็บ (HTML)
style.css    ·  ตกแต่งหน้าตา (CSS)
script.js    ·  ทำให้โต้ตอบได้ (JavaScript)
```

ทั้ง 3 ไฟล์ถูกเชื่อมกันไว้แล้วใน `index.html`
(`<link>` ดึง CSS · `<script>` ดึง JS) · ไม่ต้องตั้งค่าอะไรเพิ่ม

## วิธีใช้

1. **เอาโค้ดมา** · กดปุ่มเขียว `Code` แล้ว `Download ZIP` (หรือถ้าใช้ git เป็น):
   ```bash
   git clone https://github.com/ExtraQube/roadtoce-starter.git
   ```
2. **เปิดเว็บ** · ดับเบิลคลิกไฟล์ `index.html` เปิดด้วยเบราว์เซอร์ (Chrome/Edge)
3. **ลองแก้** · เปิด 3 ไฟล์ด้วย Text Editor (แนะนำ [VS Code](https://code.visualstudio.com/)) แล้วลองแก้:
   - เปลี่ยนข้อความใน `index.html`
   - เปลี่ยนสีใน `style.css`
   - เปลี่ยนสิ่งที่เกิดขึ้นตอนกดปุ่มใน `script.js`
4. **เซฟแล้วรีเฟรช** หน้าเว็บเพื่อดูผลลัพธ์

## ลองทำต่อ

- เปลี่ยนสีปุ่มใน `style.css` (ลองใส่ `background: green;`)
- เพิ่มหัวข้อ `<h2>` ใหม่ใน `index.html`
- ใน `script.js` ลองเปลี่ยน `count = count + 1` เป็น `count = count + 10`

อยากเรียนทีละหัวข้อแบบมีตัวอย่างกดดูผลได้ทันที → <https://roadtoce.extraqube.com>
